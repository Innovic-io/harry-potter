import { SearchCharactersPipe } from './search-characters.pipe';

describe('SearchCharactersPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCharactersPipe();
    expect(pipe).toBeTruthy();
  });
});
