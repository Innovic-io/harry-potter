import { SearchSpellsPipe } from './search-spells.pipe';

describe('SearchSpellsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSpellsPipe();
    expect(pipe).toBeTruthy();
  });
});
